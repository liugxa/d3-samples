
jDependency.imageOnclick = function(context, id, type){
	console.log("image on click! id:" + id, ", type:" + type);
}

jDependency.textOnclick = function(context, id){
	console.log("text on click! id:" + id);
}